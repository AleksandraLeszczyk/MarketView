# Core
streamlit>=1.33.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
websocket-client>=1.6.0
python-dotenv>=1.0.0
yfinance>=1.2.2,<1.3.0
curl_cffi>=0.15
lxml>=5.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
requests-mock>=1.11.0

# Optional — ML predicted price profile (agent_stonks/profile_model.py).
# Loads the LevelsML density model pack from ../Models; the chart overlay
# silently stays off without this dependency or the pack file.
lightgbm>=4.0.0

# Optional — momentum-change model behind the Apple Trader personality
# (agent_stonks/momentum_model.py, apple_trader.py). The saved bundle is a
# pickled sklearn pipeline, so unpickling it needs the same major version it
# was fitted with; without these the personality reports the missing model and
# stops instead of trading.
scikit-learn>=1.9.0
joblib>=1.4.0

# Optional — LLM pipeline (agent_stonks/llm.py, news.py, agent.py)
openai>=1.0.0
anthropic>=0.34.0
worldnewsapi>=2.0.0
pydantic>=2.0.0

# Optional — LLM observability (agent_stonks/observability.py). No-op unless
# LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY (and optionally LANGFUSE_HOST) are set.
langfuse>=3.0.0
